<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quotes</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your CSS file if applicable -->
</head>
<body>
    <h1>Quotes</h1>

    <!-- Form to submit new quotes -->
    <form action="/quotes" method="POST">
        <label for="quote">Enter a new quote:</label>
        <input type="text" name="quote" id="quote" required>
        <button class="submit" type="submit">Submit</button>
    </form>

    <!-- List of existing quotes -->
    <ul class="quotes">
        <% if (quotes.length > 0) { %> <!-- Check if there are quotes -->
            <% for (let i = 0; i < quotes.length; i++) { %>
                <li class="quote">
                    <span><%= quotes[i].quote %></span> <!-- Use EJS syntax to display the quote -->
                </li>
            <% } %>
        <% } else { %>
            <li>No quotes found.</li> <!-- Message if no quotes are available -->
        <% } %>
    </ul>
</body>
</html>
